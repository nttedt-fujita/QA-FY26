# Phase 3: 対応方針と変更計画

**作成**: Session 286 (2026-03-23)

---

## 採用方針

**案A: グローバルCLAUDE.md削減**を採用

### 理由

1. 変更量が小さく、1セッションで完了可能
2. SSOT（Single Source of Truth）を実現できる
3. ルール統合は後から追加で実施可能

---

## 変更対象と内容

### 1. グローバルCLAUDE.md（P1-1対応）

**現状**: 132行（詳細ルール + スキル一覧 + エージェント一覧 + コマンド一覧）

**目標**: 約70行（インデックス + 参照リンク）

| 削除対象 | 行数 | 移譲先 |
|----------|------|--------|
| 基本方針（詳細） | 8行 | ルール01に既存 → 参照リンクに置換 |
| 禁止ワード（詳細） | 6行 | ルール01に既存 → 参照リンクに置換 |
| コーディングスタイル | 5行 | ルール02に既存 → 参照リンクに置換 |
| ADR管理 | 2行 | ルール10に既存 → 参照リンクに置換 |
| 作業サイクル（詳細） | 12行 | ルール03に既存 → 参照リンクに置換 |
| セッション開始ルール | 8行 | ルール04に既存 → 参照リンクに置換 |

**変更後の構成**:
```
# 個人グローバル設定

## ルール適用（詳細は rules/*.md 参照）

以下のルールが全セッションで自動適用される:

| ルール | 概要 |
|--------|------|
| 01-core-principles | 推測禁止、段階的検証、禁止ワード |
| 02-coding-style | コーディングスタイル |
| 03-work-cycle | 作業サイクル（探索→計画→成果物→コミット） |
| 04-session-start | セッション開始時の必須処理 |
| ... | ... |

## スキル一覧
（現状維持）

## エージェント一覧
（現状維持）

## コマンド一覧
（現状維持）
```

---

### 2. ルール04（P1-2対応）

**現状**: 詳細手順をルール04とsession-managementスキルの両方に記載

**変更**: ルール04を「スキル呼び出しのトリガー」に限定

**変更後**:
```markdown
# セッション開始時の必須処理（常時適用）

**IMPORTANT - MUST（自動実行）**: セッション開始時、ユーザーの最初の発言を受けたら、
他の作業より先に `session-management` スキルを実行すること。

詳細手順は `~/.claude/skills/session-management/SKILL.md` に記載。
```

---

### 3. ルール07 vs 14（P2対応）

**変更**: ルール14のセクション5（二重管理禁止）を削除し、ルール07への参照に置換

**ルール14 変更箇所**:
```markdown
## 5. 二重管理の禁止

→ `07-information-routing.md` 参照
```

---

## 実施順序

1. **CLAUDE.md削減**（P1-1）
2. **ルール04簡素化**（P1-2）
3. **ルール14修正**（P2）
4. **動作確認**（新セッションで正常に動作するか）

---

## 影響範囲

| 変更対象 | 影響 |
|----------|------|
| CLAUDE.md | トークン消費削減（約60行削減） |
| ルール04 | 詳細手順がスキルに集約 |
| ルール14 | 二重管理禁止の記述が07に集約 |

**リスク**: 低（既存の情報を削除するのではなく、参照に置換するため）

---

## 実施結果

✅ Phase 4（実施）完了

| 変更対象 | Before | After |
|----------|--------|-------|
| CLAUDE.md | 132行 | 100行 |
| ルール04 | 30行 | 12行 |
| ルール14 セクション5 | 14行 | 1行 |

---

*作成: Session 286 (2026-03-23)*
